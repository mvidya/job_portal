<h1>Showing Jobs details</h1>
<% if flash[:notice] %>
<div class="notice"><%= flash[:notice] %> </div>
<% end %>
<table>
	<tr>
		<th>company_name</th>
		<th>job_type_id</th>
		<th>job_posted_on</th>
		<th>job_expires_on</th>
		<th>salary</th>
		<th>functional_area</th>
		<th>experience</th>
		<th>Action</th>
	</tr>
	<% @jobs.each do |e| %>
	<tr>
		<td><%= e.employer.company_name %></td>
		<td><%= e.job_type_id %></td>
		<td><%= e.job_posted_on %></td>
		<td><%= e.job_expires_on %></td>
		<td><%= e.salary %></td>
		<td><%= e.functional_area %></td>
		<td><%= e.experience %></td>
		<td><%= link_to "show", job_path(e) %></td></tr>
	</tr>
	<% end %>
</table>

<style>
	table, tr, th, td {
		border: 1px solid black;
	} 
	.notice{
	color:#3ABf00;
	h5{
		color:#3ABf00;
	}
}
</style>
<h5><%= link_to "home", employer_dashboard_home_index_path %></h5>